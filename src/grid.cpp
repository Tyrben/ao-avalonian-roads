#include "grid.h"
/*
bool Grid::addPortal(const Portal& portal_, Coordinates coords_)
{
	m_portals[portal_.getId()] = coords_;
	return true;
}

bool Grid::addPortal(const PortalId& portalId_, Coordinates coords_)
{
	m_portals[portalId_] = coords_;
	return true;
}

int Grid::getAbsoluteDistance(const Portal& fromPortal_, const Portal& toPortal_) const
{
	return 0;
}

int Grid::getAbsoluteDistance(const PortalId& fromPortal_, const PortalId& toPortal_) const
{
	m_portals.find(fromPortal_);

	return 0;
}

const std::map<PortalId, Coordinates>& Map::getPortals() const
{
	return m_portals;
}
*/