#include "map.h"

int Map::getAbsoluteDistance(const Portal& fromPortal_, const Portal& toPortal_) const
{

}

int Map::getAbsoluteDistance(const Id<Portal>& fromPortal_, const Id<Portal>& toPortal_) const
{
	m_portals.find(fromPortal_);
}
